//! Recording — DEPRECATED: singleton removed in unified output model.
//!
//! Recording functionality is now handled per-output via `FfmpegSubprocess` in
//! `src/renderer/subprocess.rs`. This module is kept empty to avoid breaking
//! the module declaration until fully cleaned up.
